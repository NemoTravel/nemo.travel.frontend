.nemo-common-sticker {
	position: relative;
	display: block;
	//height: 100%;
	align-self: stretch;
	padding: 0;
	//max-height: 100%;
	//overflow: hidden;

	&__inner {
		//padding: 20px;
		overflow: hidden;
		position: absolute;
		width: 100%;
		margin: 0 0;
		max-height: 100%;
	}
}